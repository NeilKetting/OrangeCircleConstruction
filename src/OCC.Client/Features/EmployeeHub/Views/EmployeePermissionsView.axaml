<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:OCC.Client.Features.EmployeeHub.ViewModels" xmlns:timeVm="using:OCC.Client.Features.TimeAttendanceHub.ViewModels"
             mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="550"
             x:Class="OCC.Client.Features.EmployeeHub.Views.EmployeePermissionsView"
             x:DataType="vm:EmployeePermissionsViewModel">

    <Border Background="White" CornerRadius="8" Padding="25" BoxShadow="0 4 20 0 #40000000">
        <StackPanel Spacing="20">
            <StackPanel Spacing="5">
                <TextBlock Text="Edit Access &amp; Permissions" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryDark}"/>
                <TextBlock Text="{Binding UserName, StringFormat='For Employee: {0}'}" FontSize="14" Foreground="{DynamicResource TextSecondaryGray}"/>
            </StackPanel>

            <Separator Background="{DynamicResource BorderGray}"/>

            <TextBlock Text="View Permissions" Classes="FormLabel" Margin="0,10,0,0"/>
            <ScrollViewer Height="250">
                <ItemsControl ItemsSource="{Binding Permissions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding DisplayName}" Margin="0,5" Padding="10,0"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right">
                <Button Content="Save Access Settings" Command="{Binding SaveCommand}" Classes="Primary"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
