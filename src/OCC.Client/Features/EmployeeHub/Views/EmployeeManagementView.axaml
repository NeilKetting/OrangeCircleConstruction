<UserControl xmlns="https://github.com/avaloniaui"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:vm="clr-namespace:OCC.Client.Features.EmployeeHub.ViewModels"
			 xmlns:models="using:OCC.Shared.Models"
			 xmlns:views="clr-namespace:OCC.Client.Features.EmployeeHub.Views"
             xmlns:timeViews="clr-namespace:OCC.Client.Features.TimeAttendanceHub.Views"
			 xmlns:conv="clr-namespace:OCC.Client.Converters"
			 mc:Ignorable="d" d:DesignWidth="1200"
			 x:Class="OCC.Client.Features.EmployeeHub.Views.EmployeeManagementView"
			 x:DataType="vm:EmployeeManagementViewModel">

	<UserControl.Resources>
		<conv:StringEqualityConverter x:Key="StringEqualityConverter"/>
	</UserControl.Resources>

	<Design.DataContext>
		<vm:EmployeeManagementViewModel/>
	</Design.DataContext>

	<Grid RowDefinitions="Auto, *">
		<!-- Top Bar -->
		<Border Grid.Row="0" Classes="Topbar">
			<Grid ColumnDefinitions="Auto, *, Auto, Auto" Background="{DynamicResource TopMenuGradient}">
				<!-- Tab Buttons -->
				<StackPanel Grid.Column="0" Orientation="Horizontal" Grid.ColumnSpan="2" HorizontalAlignment="Center">
					<Button Classes="MenuTabButton" Command="{Binding SetActiveTabCommand}" 
							CommandParameter="Employees"
							Classes.Active="{Binding ActiveTab, 
							Converter={StaticResource StringEqualityConverter}, 
							ConverterParameter='Employees'}">
						<StackPanel Orientation="Horizontal" Spacing="8">
							<PathIcon Data="{StaticResource IconManageEmployees}" Height="16"/>
							<TextBlock Text="MANAGE EMPLOYEES"/>
						</StackPanel>
					</Button>

					<Button Classes="MenuTabButton" Command="{Binding SetActiveTabCommand}" CommandParameter="Teams"
							Classes.Active="{Binding ActiveTab, Converter={StaticResource StringEqualityConverter}, ConverterParameter='Teams'}">
						<StackPanel Orientation="Horizontal" Spacing="8">
							<PathIcon Data="{StaticResource IconTeam}" Height="16"/>
							<TextBlock Text="TEAMS"/>
						</StackPanel>
					</Button>
				</StackPanel>
			</Grid>
		</Border>
		

        <!-- Main Content Area -->
        <TransitioningContentControl Grid.Row="1" Content="{Binding CurrentContent}">
            <TransitioningContentControl.PageTransition>
                <CrossFade Duration="0:00:00.500" />
            </TransitioningContentControl.PageTransition>
            <TransitioningContentControl.DataTemplates>
                <DataTemplate DataType="{x:Type vm:EmployeeManagementViewModel}">
                    <views:EmployeeListView />
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:TeamManagementViewModel}">
                    <views:TeamsView />
                </DataTemplate>
            </TransitioningContentControl.DataTemplates>
        </TransitioningContentControl>

		<!-- Popup Overlay -->
		<Grid Grid.Row="0" Grid.RowSpan="3" Background="#80000000" IsVisible="{Binding IsAddEmployeePopupVisible}">
			<Grid Background="#20000000">
				<views:EmployeeDetailView x:Name="DetailView" DataContext="{Binding AddEmployeePopup}"
										  HorizontalAlignment="Center"
										  VerticalAlignment="Center"/>
			</Grid>
		</Grid>

		<!-- Team Popup Overlay -->
		<Grid Grid.Row="0" Grid.RowSpan="3" Background="#80000000" IsVisible="{Binding IsAddTeamPopupVisible}">
			<Grid Background="#20000000">
				<views:TeamDetailView x:Name="TeamDetailView" DataContext="{Binding TeamDetailPopup}"
									  HorizontalAlignment="Center"
									  VerticalAlignment="Center"/>
			</Grid>
		</Grid>

        <!-- Employee Report Popup Overlay -->
        <Grid Grid.Row="0" Grid.RowSpan="3" Background="#80000000" IsVisible="{Binding IsEmployeeReportPopupVisible}">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                 <!-- Main Report View -->
                 <timeViews:EmployeeReportView DataContext="{Binding EmployeeReportPopup}" 
                                           MaxWidth="1100" MaxHeight="850"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Background="White"
                                           CornerRadius="8"/>
                 
                 <!-- Close Button (Top Right) -->
                 <Button Classes="Close" 
                         Command="{Binding CloseReportCommand}"
                         HorizontalAlignment="Right" VerticalAlignment="Top" 
                         Margin="0,5,5,0">
                     <PathIcon Data="{StaticResource IconClose}" />
                 </Button>
            </Grid>
        </Grid>
	</Grid>
</UserControl>
