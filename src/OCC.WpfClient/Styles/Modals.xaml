<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Standard Detail View Modal Border -->
    <Style x:Key="DetailViewModalBorder" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBackground}" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="BorderBrush" Value="{StaticResource OrangeBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="30" ShadowDepth="5" Opacity="0.5"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Standard Detail View Header -->
    <Style x:Key="DetailViewHeader" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BackgroundDark}" />
        <Setter Property="CornerRadius" Value="12,12,0,0" />
        <Setter Property="Padding" Value="25,20" />
    </Style>

    <!-- Standard Detail View Content ScrollViewer -->
    <Style x:Key="DetailViewContentScroll" TargetType="ScrollViewer">
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="Padding" Value="25,20" />
    </Style>

    <!-- Standard Detail View Footer -->
    <Style x:Key="DetailViewFooter" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BackgroundDark}" />
        <Setter Property="Padding" Value="25,20" />
        <Setter Property="CornerRadius" Value="0,0,12,12" />
    </Style>

    <!-- Section Header Text Style -->
    <Style x:Key="SectionHeaderText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- Section Icon Style -->
    <Style x:Key="SectionIcon" TargetType="Path">
        <Setter Property="Fill" Value="{StaticResource AccentBlue}" />
        <Setter Property="Width" Value="20" />
        <Setter Property="Height" Value="20" />
        <Setter Property="Stretch" Value="Uniform" />
        <Setter Property="Margin" Value="0,0,10,0" />
    </Style>

</ResourceDictionary>
