<Window x:Class="OCC.WpfClient.Infrastructure.Views.Dialogs.CustomDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dialog" MinHeight="220" Width="550" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False">
    <Border Background="{StaticResource CardBackground}" CornerRadius="10" 
            BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5" />
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <Path Data="{StaticResource IconAlert}" Fill="#ffb020" Width="24" Height="24" Stretch="Uniform" Margin="0,0,10,0"/>
                <TextBlock x:Name="TitleText" Text="Conflict Detected" Foreground="White" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
            
            <TextBlock x:Name="MessageText" Grid.Row="1" Text="Message goes here." Foreground="{StaticResource TextSub}" TextWrapping="Wrap" FontSize="14" />
            
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button x:Name="BtnCancel" Content="Cancel" Style="{StaticResource CancelButton}" Width="80" Height="35" Margin="0,0,10,0" Click="BtnCancel_Click"/>
                <Button x:Name="BtnSecondary" Content="Reload" Style="{StaticResource SecondaryButton}" Width="150" Height="35" Margin="0,0,10,0" Click="BtnSecondary_Click" />
                <Button x:Name="BtnPrimary" Content="Force Save" Style="{StaticResource PrimaryButton}" Width="150" Height="35" Click="BtnPrimary_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>

