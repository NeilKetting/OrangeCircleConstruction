<UserControl x:Class="OCC.WpfClient.Features.Main.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Greeting -->
            <RowDefinition Height="Auto" /> <!-- Summary Cards -->
            <RowDefinition Height="*" />    <!-- Lists/Details -->
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Foreground="White" FontSize="28" FontWeight="SemiBold">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0}, {1}">
                        <Binding Path="Greeting" />
                        <Binding Path="UserName" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
            <TextBlock Text="{Binding CurrentDate}" Style="{StaticResource FormLabelText}"/>
        </StackPanel>

        <!-- Summary Cards -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,40">
            <!-- Tasks Card -->
            <Border Style="{StaticResource GlassCard}" Margin="0,0,10,0" Padding="25">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="TASKS" Foreground="{StaticResource TextSub}" FontSize="12" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TaskCount}" Foreground="White" FontSize="42" FontWeight="Bold" Margin="0,10,0,5"/>
                        <TextBlock Text="Total Active" Foreground="{StaticResource TextSub}" FontSize="13"/>
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Top" Width="40" Height="40" CornerRadius="12" Background="#1A2E9DFF">
                        <TextBlock Text="" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource AccentBlue}" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- To-Dos Card -->
            <Border Style="{StaticResource GlassCard}" Margin="5,0" Padding="25">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="TO-DOS" Foreground="{StaticResource TextSub}" FontSize="12" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TodoCount}" Foreground="White" FontSize="42" FontWeight="Bold" Margin="0,10,0,5"/>
                        <TextBlock Text="Pending Review" Foreground="{StaticResource TextSub}" FontSize="13"/>
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Top" Width="40" Height="40" CornerRadius="12" Background="#1AFF8F00">
                        <TextBlock Text="" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource OrangeBrush}" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- User Management Card -->
            <Border Style="{StaticResource GlassCard}" Margin="5,0" Padding="25">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="USERS" Foreground="{StaticResource TextSub}" FontSize="12" FontWeight="Bold"/>
                        <TextBlock Text="{Binding UserCount}" Foreground="White" FontSize="42" FontWeight="Bold" Margin="0,10,0,5"/>
                        <TextBlock Text="Total Registered" Foreground="{StaticResource TextSub}" FontSize="13"/>
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Top" Width="40" Height="40" CornerRadius="12" Background="#1A6B39FF">
                        <TextBlock Text="" FontFamily="Segoe MDL2 Assets" Foreground="#BE9DFF" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Productivity Card -->
            <Border Style="{StaticResource GlassCard}" Margin="10,0,0,0" Padding="25">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="PRODUCTIVITY" Foreground="{StaticResource TextSub}" FontSize="12" FontWeight="Bold"/>
                        <TextBlock Text="84%" Foreground="White" FontSize="42" FontWeight="Bold" Margin="0,10,0,5"/>
                        <TextBlock Text="Trend: +12%" Foreground="#49FF8F00" FontSize="13" FontWeight="SemiBold"/>
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Top" Width="40" Height="40" CornerRadius="12" Background="#1A00FF88">
                        <TextBlock Text="" FontFamily="Segoe MDL2 Assets" Foreground="#00FF88" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>
        </UniformGrid>

        <!-- Lists -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource GlassCard}" Margin="0,0,15,0" Padding="25">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <TextBlock Text="Due Today" Foreground="White" FontSize="18" FontWeight="SemiBold"/>
                        <Border Background="{StaticResource AccentBlue}" CornerRadius="10" Padding="8,2" Margin="15,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="2" Foreground="White" FontSize="12" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                    
                    <StackPanel Grid.Row="1">
                        <Border Background="#0DFFFFFF" CornerRadius="12" Padding="20,15" Margin="0,0,0,12">
                            <Grid>
                                <StackPanel>
                                    <TextBlock Text="Meet1" Foreground="White" FontSize="15" FontWeight="SemiBold"/>
                                    <TextBlock Text="Started • Neil Ketting" Style="{StaticResource FormLabelText}"/>
                                </StackPanel>
                                <TextBlock Text="HIGH" HorizontalAlignment="Right" Foreground="#FF4B4B" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource GlassCard}" Margin="15,0,0,0" Padding="25">
                <StackPanel>
                    <TextBlock Text="Personal To-Dos" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"/>
                    <TextBlock Text="No to-do's today" Foreground="{StaticResource TextSub}" FontSize="14" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,40,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>

