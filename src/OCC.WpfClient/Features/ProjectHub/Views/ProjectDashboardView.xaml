<UserControl x:Class="OCC.WpfClient.Features.ProjectHub.Views.ProjectDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.ProjectHub.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance Type=vm:ProjectDashboardViewModel}">

    <Grid Margin="0" Background="{StaticResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Margin="10">
            <Grid>
                <StackPanel>
                    <TextBlock Text="Project Dashboard" FontSize="26" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Monitor construction progress, budgets and timelines across your portfolio." 
                               Style="{StaticResource FormLabelText}"/>
                </StackPanel>

                <Button HorizontalAlignment="Right" VerticalAlignment="Top" 
                        Command="{Binding CloseCommand}" Style="{StaticResource WindowCloseButton}"/>
            </Grid>
        </Border>

        <!-- Stats Grid -->
        <UniformGrid Grid.Row="1" Columns="3" Margin="10">
            <Border Style="{StaticResource GlassCard}" Margin="0,0,20,0" Padding="25">
                <StackPanel>
                    <TextBlock Text="Active Projects" Foreground="{StaticResource TextSub}" FontSize="12" FontWeight="Bold"/>
                    <TextBlock Text="{Binding ActiveProjectCount}" Foreground="White" FontSize="42" FontWeight="Bold" Margin="0,10,0,5"/>
                    <ProgressBar Value="75" Height="4" Margin="0,15,0,0" Foreground="{StaticResource AccentBlue}" Background="#20FFFFFF"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource GlassCard}" Margin="0,0,20,0" Padding="25">
                <StackPanel>
                    <TextBlock Text="Overdue Tasks" Foreground="{StaticResource TextSub}" FontSize="12" FontWeight="Bold"/>
                    <TextBlock Text="{Binding OverdueTaskCount}" Foreground="{StaticResource ErrorRed}" FontSize="42" FontWeight="Bold" Margin="0,10,0,5"/>
                    <TextBlock Text="Requires immediate attention" FontSize="11" Foreground="{StaticResource TextSub}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource GlassCard}" Padding="25">
                <StackPanel>
                    <TextBlock Text="Avg. Completion" Foreground="{StaticResource TextSub}" FontSize="12" FontWeight="Bold"/>
                    <TextBlock Text="{Binding CompletionRate, StringFormat='{}{0:P0}'}" Foreground="{StaticResource SuccessGreen}" FontSize="42" FontWeight="Bold" Margin="0,10,0,5"/>
                    <TextBlock Text="Overall portfolio health" FontSize="11" Foreground="{StaticResource TextSub}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Main Content Area -->
        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Navigation / Quick Actions -->
            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                <TextBlock Text="Explore Portfolio" FontSize="20" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,20"/>
                
                <Button Command="{Binding GoToRegistryCommand}" Style="{StaticResource ListActionButtonStyle}" Height="120" Margin="0,0,0,20">
                    <Grid Margin="20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Path Data="{StaticResource IconList}" Fill="{StaticResource AccentBlue}" Stretch="Uniform" Width="40" Height="40" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Projects" FontSize="20" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="View and search the complete database of construction projects." Foreground="{StaticResource TextSub}"/>
                        </StackPanel>
                    </Grid>
                </Button>

                <Button Style="{StaticResource ListActionButtonStyle}" Height="120">
                    <Grid Margin="20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Path Data="{StaticResource IconCalendar}" Fill="{StaticResource SuccessGreen}" Stretch="Uniform" Width="40" Height="40" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Gantt Timeline" FontSize="20" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="Visualize project schedules and task dependencies." Foreground="{StaticResource TextSub}"/>
                        </StackPanel>
                    </Grid>
                </Button>
            </StackPanel>

            <!-- Recent Activity Placeholder -->
            <Border Grid.Column="1" Style="{StaticResource GlassCard}" Padding="20">
                <StackPanel>
                    <TextBlock Text="Recent Updates" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,20"/>
                    <TextBlock Text="New project 'Engen Bendor' created." FontSize="14" Foreground="{StaticResource TextSub}" Margin="0,0,0,12"/>
                    <TextBlock Text="Task 'Site Clearance' completed by Neil." FontSize="14" Foreground="{StaticResource TextSub}" Margin="0,0,0,12"/>
                    <TextBlock Text="Budget update for 'Mall Expansion'." FontSize="14" Foreground="{StaticResource TextSub}" Margin="0,0,0,12"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
