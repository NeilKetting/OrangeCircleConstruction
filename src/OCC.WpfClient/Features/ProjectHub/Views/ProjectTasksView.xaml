<UserControl x:Class="OCC.WpfClient.Features.ProjectHub.Views.ProjectTasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.ProjectHub.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             d:DataContext="{d:DesignInstance vm:ProjectTasksViewModel}">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tool Bar -->
        <Border Grid.Row="0" Margin="10">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Project Tasks" FontSize="20" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                    <Border Background="{StaticResource AccentBlue}" CornerRadius="12" Padding="10,2" Margin="15,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Tasks.Count}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                    </Border>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="New Task" Style="{StaticResource ActionButtonPrimary}" Width="120"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Tasks Grid -->
        <Border Grid.Row="1"  Style="{StaticResource GlassCard}" Padding="0" CornerRadius="0" Margin="10,0,10,10">
            <Grid>
                <!-- Empty State -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{Binding HasTasks, Converter={StaticResource VisibilityConverter}, ConverterParameter='Invert'}">
                    <Path Data="{StaticResource IconCheckCircle}" Fill="{StaticResource TextSub}" Width="48" Height="48" Stretch="Uniform" Opacity="0.5"/>
                    <TextBlock Text="No tasks found for this project" Foreground="{StaticResource TextSub}" FontSize="16" Margin="0,20,0,0" HorizontalAlignment="Center"/>
                </StackPanel>

                <DataGrid ItemsSource="{Binding Tasks}"
                          SelectedItem="{Binding SelectedTask}"
                          Visibility="{Binding HasTasks, Converter={StaticResource VisibilityConverter}}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          Background="Transparent"
                          BorderThickness="0"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#0DFFFFFF"
                          RowBackground="Transparent"
                          CanUserAddRows="False">
                    <DataGrid.Columns>
                        <!-- Status Indicator -->
                        <DataGridTemplateColumn Width="40">
                             <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Ellipse Width="8" Height="8" Grid.Column="0" VerticalAlignment="Center">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Fill" Value="{StaticResource TextSub}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                        <Setter Property="Fill" Value="{StaticResource SuccessGreen}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="In Progress">
                                                        <Setter Property="Fill" Value="{StaticResource AccentBlue}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="TASK NAME" Binding="{Binding Name}" Width="*" MinWidth="150" ElementStyle="{StaticResource DataGridTextBoldStyle}"/>

                        <DataGridTemplateColumn Header="STATUS" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="#10FFFFFF" CornerRadius="4" Padding="8,2" HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding Status}" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="PROGRESS" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel VerticalAlignment="Center" Margin="0,0,20,0">
                                        <ProgressBar Value="{Binding PercentComplete}" Height="4" BorderThickness="0" Background="#15FFFFFF" Foreground="{StaticResource SuccessGreen}"/>
                                        <TextBlock Text="{Binding PercentComplete, StringFormat='{}{0}%'}" Foreground="{StaticResource TextSub}" FontSize="10" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="DUE DATE" Binding="{Binding FinishDate, StringFormat=\{0:dd MMM yyyy\}}" Width="150" ElementStyle="{StaticResource DataGridTextStyle}"/>

                        <DataGridTemplateColumn Width="Auto" MinWidth="50">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,0">
                                        <Button Style="{StaticResource IconButtonSmall}" Command="{Binding DataContext.EditTaskCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}">
                                            <Path Data="{StaticResource IconEdit}" Fill="White" Width="14" Height="14" Stretch="Uniform" Opacity="0.6"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
